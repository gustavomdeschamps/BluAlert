{
  "name": "blualert-server",
  "version": "1.0.0",
  "private": true,
  "description": "Servidor local do BluAlert para dados oficiais do AlertaBlu e versão web.",
  "scripts": { "start": "node --use-system-ca server/app.js" },
  "engines": { "node": ">=20" }
}
